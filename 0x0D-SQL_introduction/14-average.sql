-- computes the score average of all records in second_table of hbtn_0c_0
-- The column name should be average
-- passed as an argument of the mysql command
SELECT AVG(score) AS average FROM second_table;
